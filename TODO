==THe Almighty To Do list==
* Create a twisted backend
* Move support from q.py to backends/async.py for NOTICES, MODES, CODES and so on
* Convert the go method of all modules to privmsg
* Convert the use of self.append to self.msg
* Allow the choice of a backend in the config
* Complete the movement of api.py from BBot 5 to BBot 6 with speed enhancements and support for the new backend choice

===Module Changes Needed===
* Have BlockBot() use a sqlite database in memory